# Core versions used for the recorded H100 MIG runs. Install PyTorch from your
# matching CUDA wheel source; the test environment used torch 2.8.0+cu129.
torch==2.8.0
transformers==5.3.0
tokenizers==0.22.2
safetensors==0.7.0
numpy==1.26.4
pydantic==2.12.5
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.53.0
pytest==9.1.1
httpx==0.28.1
